import { CessationScopePipePipe } from './cessation-scope.pipe.pipe';

describe('CessationScopePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CessationScopePipePipe();
    expect(pipe).toBeTruthy();
  });
});
