import { InjectionToken } from '@angular/core';
import { UntypedFormGroup } from '@angular/forms';

export const PERMIT_TRANSFER_A_FORM = new InjectionToken<UntypedFormGroup>('Permit transfer transferring form');
