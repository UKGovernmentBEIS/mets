import { InjectionToken } from '@angular/core';
import { UntypedFormGroup } from '@angular/forms';

export const PERMIT_NOTIFICATION_TASK_FORM = new InjectionToken<UntypedFormGroup>('Permit notification task form');
