server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  error_page 404 = @not_found;

  location = /installation-aviation/index.html {
    try_files $uri @not_found;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
  }

  location ~* ^/installation-aviation/(?:assets|hmcts-assets)/ {
    try_files $uri @not_found;
    add_header Cache-Control "public, max-age=3600, must-revalidate" always;
  }

  location ~* "^/installation-aviation/[^/]+-[a-zA-Z0-9]{8,}\.(?:js|css|mjs)(?:\.map)?$" {
    try_files $uri @not_found;
    add_header Cache-Control "public, max-age=31536000, immutable" always;
  }

  location ~* ^/installation-aviation/.+\.(?:js|css|mjs|map|json|html|ico|png|jpg|jpeg|gif|svg|webp|woff|woff2|ttf|eot|xlsx|pdf|txt)$ {
    try_files $uri @not_found;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
  }

  location /installation-aviation/ {
    try_files $uri $uri/ /installation-aviation/index.html;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
  }

  location / {
    return 301 /installation-aviation/;
  }

  location @not_found {
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
    return 404;
  }

  include /etc/nginx/extra-conf.d/*.conf;
}
